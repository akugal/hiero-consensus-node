global:
  scrape_interval: 1s

scrape_configs:
  - job_name: 'prom-scrap'
    metrics_path: '/metrics'
    static_configs:
      # TODO scrape two endpoints for now
      - targets: ['host.docker.internal:9999', 'host.docker.internal:8888']